package org.pknu.weather.common;
/*
import lombok.extern.slf4j.Slf4j;
import org.junit.jupiter.api.Test;
import org.pknu.weather.common.utils.S3UploaderUtils;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
@Slf4j
public class S3ImageUploadTest {

    @Autowired
    private S3UploaderUtils s3UploaderUtils;

    @Test
    public void testUpload(){
        try {
            String filePath = "/Users/seongsiyeong/Desktop/uploadImg/testImage.png";
            String uploadName = s3UploaderUtils.upload(filePath);
            log.info(uploadName);
        } catch (Exception e) {
            log.error(e.getMessage());
        }
    }

    @Test
    public void testRemove() {
        try {
            s3UploaderUtils.removeS3File("testImage.png");
        } catch (Exception e) {
            log.error(e.getMessage());
        }
    }
}

 */
